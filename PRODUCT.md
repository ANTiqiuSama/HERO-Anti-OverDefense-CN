# HERO Guard：从提示词到运行时范围控制

## 结论

HERO 最合适的产品形态不是一段越来越长的提示词，也不是每隔几分钟弹一次提醒，而是一个分层的范围控制器：

1. **开始前编译目标**：把方向、阶段、资源限制和完成条件分开；
2. **运行中只盯关键事件**：用户纠偏、阶段变化、重复治理产物和高成本动作；
3. **结束后回放**：判断工作是否真的推进主交付物，而不是看 Agent 有没有复述规则。

当前仓库先交付最低成本的 Repo Kit：短 `AGENTS.md`、按需 Skill 和案例回归。验证有效后，再把 Hook、状态和回放打包成插件。

## 一个匿名长任务暴露了什么

下面是一次持续约 12 天的研究型 Agent 任务快照。它不是效果评分，只用来定位循环形状：

| 观测项 | 数量 |
|---|---:|
| 用户消息 | 60 |
| Agent 进度/结果消息 | 2,917 |
| 补丁应用事件 | 1,551 |
| `exec` 调用记录 | 13,211 |
| 上下文压缩 | 107 |
| 累计 token | 约 24.91 亿 |

累计 token 中约 24.38 亿是缓存输入，所以不能把总数直接当成新生成成本；事件也不等于独立概念动作。但它们共同说明，这已经不是偶发的一次“多做检查”，而是一个会自我延续的执行系统。

对话里出现了三个阶段：

1. 用户给出“持续改进、追求 SOTA、达到顶刊标准”的长期方向，同时要求保留成果、独立测试、冻结模型和可复现包；
2. Agent 把严谨要求扩展到大量版本、冻结脚本、哈希、清单、审计和回归；
3. 用户明确要求“减少无效封装，只用已有数据，不再下载”。之后零下载约束基本得到执行，但顶层 Goal 没有同步改写，版本化治理仍继续运行。

在纠偏后的快照中，仍有 121 条 Agent 消息、533 次 `exec` 调用和 96 个文件变更条目，并新增了 V20、V23、V24、V25 的冻结脚本及对应测试；版本叙事后来继续到 V27。

这不是说这些工作全部无效。独立测试前冻结候选、防止外测标签泄漏、保留置信区间和复现实验，都属于真实科研要求；任务也确实产生过指标改进。真正的问题是：

- 探索、确认和发布没有清楚分层；
- 用户纠偏只被当成“禁止下载”执行，没有覆盖“减少封装”的上位意图；
- “持续改进 / SOTA”没有有界停止条件，版本号自然成了继续运行的理由；
- 旧 Goal 仍明确要求冻结和可复现包，短期纠偏没有被提升为新的阶段约束。

因此，单纯多提醒一次 HERO 不够。产品必须能保存最新纠偏、识别阶段，并把无限方向编译成有界实验轮次。

## 产品结构

### 1. Goal Compiler：开始前把长期目标写对

它不替用户制定研究结论，只检查 Goal 是否缺少执行边界：

- “持续改进”“做到 SOTA”“达到顶刊标准”是否被误写成完成条件；
- 当前是探索、确认还是发布；
- 这一轮的假设、指标、资源边界和保留/停止条件是什么；
- “保留全部成果”“每版冻结”“禁止覆盖”等要求是否会让探索阶段变成 append-only 工程；
- 新指令是否与旧 Goal 冲突。

输出应是一份短任务合同，不是新的长计划：主交付物、当前阶段、硬约束、下一决策点、结束条件。

### 2. Scope Guard：运行中按事件工作

语义判断由 Skill 完成，机器可判断的事件交给 Hook：

| 事件 | 处理 |
|---|---|
| 新任务、恢复、上下文压缩 | 注入一次当前任务合同，不重复整个案例库 |
| 用户给出明确禁止或资源限制 | 更新“纠偏锁存”，在当前阶段覆盖旧的宽泛指令 |
| 探索阶段重复新建 freeze、manifest、checksum、audit 或版本包装 | 静默触发一次范围检查 |
| 明确“禁止网络/下载”后，支持拦截的工具仍准备联网 | 阻止或改写对应调用，并说明具体约束 |
| 进入独立确认或发布 | 允许一次冻结与相称验证，清除同一决策的旧提醒 |
| 任务结束 | 生成一次回放摘要，不在过程中持续刷屏 |

Codex Hook 已支持 `SessionStart`、`UserPromptSubmit`、`PreToolUse`、`PostToolUse`、`PostCompact` 和 `Stop` 等生命周期事件，插件也可以携带 Hook。`SessionStart` 能区分 `startup`、`resume`、`clear` 和 `compact`，适合在恢复或压缩后重建锚点；`PreToolUse` 能观察并在受支持的本地工具路径上阻止或改写调用。具体能力以 OpenAI 官方 [Hooks 文档](https://learn.chatgpt.com/docs/hooks) 为准。

Hook 不是完整安全边界。部分专用或托管工具不经过同一 Hook 路径，历史 transcript 格式也不是稳定接口。因此产品不应靠解析内部 JSONL 作为长期核心，也不能宣称“拦住所有联网”。正确做法是让插件维护自己的小型事件账本，并把 Hook 当作范围护栏。

### 3. Replay：任务结束后再判断

回放报告只回答少数有用的问题：

- 主交付物在哪些节点发生了可验证变化；
- 用户纠偏后，行为用了多少步真正改变；
- 哪些检查解决了仍然活着的疑点；
- 哪些版本只增加包装，没有带来新假设或新决定；
- 是否错误压掉了真实缺陷、安全要求或必要确认性实验。

不要生成一个“过度防御总分”。用评分器替代判断，本身就可能变成 HERO-R。更合适的是列出证据和决策点，让人快速复核。

## 提醒频率

默认不使用时间间隔。一次范围提醒的去重键可以理解为：

```text
当前任务 + 当前阶段 + 决策类型 + 现有证据
```

这个组合没变，就不再提醒。只有以下情况重新打开：

- 用户目标或硬约束改变；
- 探索进入确认，或确认进入发布；
- 出现新证据；
- 会话恢复或压缩后需要重建状态；
- 辅助工作开始挤占主交付物。

用户可见的打断只发生在方案会显著增加成本、扩大范围、产生不可逆影响或需要用户选择时。普通范围检查静默完成。

## 为什么不是只做 Skill、只写 AGENTS.md 或只上 Hook

- **只做 Skill**：上下文成本低，但任务未命中描述或没有显式调用时会缺席；
- **只写 AGENTS.md**：覆盖稳定，但复杂判断和长案例会永久占上下文；
- **只上 Hook**：适合确定性的命令和事件，不擅长判断一次冻结到底是科研严谨还是流程膨胀；
- **三者组合**：`AGENTS.md` 保底，Skill 做语义判断，Hook 做状态、去重和确定性约束。

Codex 的 Skill 使用渐进加载：初始只暴露名称和描述，命中后才加载完整 `SKILL.md`，适合承载这类低频复杂流程。官方说明见 [Build skills](https://learn.chatgpt.com/docs/build-skills)。

## 最小产品路线

### M0：Repo Kit（当前）

- 短常驻合同；
- 一个按需范围 Skill；
- 包含正例和反例的案例库；
- 不新增服务、数据库或前端。

### M1：离线回放器

- 导入用户主动导出的任务记录；
- 生成主线变化、纠偏延迟和重复治理产物摘要；
- 默认本地处理，不上传私有代码和对话；
- 使用自有版本化事件格式，不承诺兼容未经文档化的内部 transcript。

### M2：HERO Guard 插件

- Skill + Hook + 本地状态合成一个可安装包；
- 状态只保留任务合同、阶段、明确硬约束和提醒去重键；
- 后台记录事件，触发时才向模型增加极短上下文；
- UI 只显示“主线 / 约束 / 当前阶段 / 最近一次范围决策”，不在聊天中刷提醒。

先用固定案例比较“无 HERO、只有 AGENTS.md、AGENTS.md + Skill”是否真的改善行为。只有 Repo Kit 的收益和误伤都可测后，插件才值得建设；否则插件本身会成为 HERO-O。
